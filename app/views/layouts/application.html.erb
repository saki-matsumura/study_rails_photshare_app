<!DOCTYPE html>
<html>
  <head>
    <title>PhotshareApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", media: "all", 'data-turbolinks-track': "reload" %>
    <%= javascript_pack_tag "application", 'data-turbolinks-track': "reload" %>
    
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">

    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_pack_tag "application" %>
  </head>

  <body>
  <header>
  <div class = "row">
    <div class = "app_name text-left col-sm mt-3">
      <p>Photo share app</p>
    </div>
    <div class = "login text-right col-sm mt-3">
      <%# ログインしていいるか否かで表示を変える %>
    <% if logged_in? %>
      <%# ログインしている時：プロフィールとログアウトボタン表示 %>
      <%= link_to "Profile", user_path(current_user.id), class: "btn btn-outline-secondary"%>
      <%= link_to "Logout", session_path(current_user.id), method: :delete, class: "btn btn-outline-danger" %>
    <% else %>
      <%# ログアウトしている時：サインアップとログインボタン表示 %>
      <%= link_to "Sign up", new_user_path, class: "btn btn-outline-primary"%>
      <%= link_to "Log in", new_session_path, class: "btn btn-outline-primary"%>
    <% end %>
    </div>
  </div>
  </header>

  <div class = "container">
    <%# フラッシュメッセージを表示 %>
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>

    <%= yield %>
  </div>
  </body>
</html>
